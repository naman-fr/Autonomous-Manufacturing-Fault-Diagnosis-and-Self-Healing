apiVersion: apps/v1
kind: Deployment
metadata:
  name: amfd-dashboard
spec:
  replicas: 2
  selector:
    matchLabels:
      app: amfd-dashboard
  template:
    metadata:
      labels:
        app: amfd-dashboard
    spec:
      containers:
        - name: dashboard
          image: amfd-dashboard:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8501
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
              nvidia.com/gpu: "1"
---
apiVersion: v1
kind: Service
metadata:
  name: amfd-dashboard
spec:
  selector:
    app: amfd-dashboard
  ports:
    - port: 80
      targetPort: 8501

